{
  "version": "0.6.3",
  "releaseNotes": [
    "Fixed edit mode so clearing a line removes it from the file instead of leaving an empty line.",
    "Whitespace-only lines remain supported when intentionally editing file formatting."
  ]
}
